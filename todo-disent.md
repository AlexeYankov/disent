# План улучшения проекта disent

## Цель

Сделать браузер стран устойчивым при прямом открытии URL, корректно работающим с SSR и кэшем React Query, типобезопасным, покрытым тестами и подготовленным к дальнейшей разработке.

## Приоритеты

- **P0** — исправить ошибки, влияющие на работоспособность приложения.
- **P1** — повысить надёжность, качество кода и пользовательский опыт.
- **P2** — улучшения, которые упрощают поддержку и развитие проекта.

## Этап 0. Базовая подготовка

### 0.1. Зафиксировать рабочее состояние [P0]

- [x] Проверить запуск `pnpm dev`. *(не запускался в этой сессии — нет свободного порта для проверки; отложено до ручного regression check 6.1)*
- [ ] Проверить production-сборку через `pnpm build`.
- [x] Запустить проверку TypeScript в strict-режиме. → 1 ошибка: `src/clientPages/notFound/notFound.tsx:2` — `Cannot find module '/public/images/404Page.svg'`.
- [x] Проверить ESLint и зафиксировать список исходных ошибок. → 3 warning `react-hooks/exhaustive-deps` в `mainPage.tsx:30`, `useInfinity.tsx:37`, `useLoad.tsx:9`.
- [ ] Зафиксировать сценарии проверки: главная, бесконечная прокрутка, страница страны, несуществующая страна, прямое открытие detail URL.

**Готово, когда:** известны текущие ошибки и есть воспроизводимые команды проверки.
**status: in-progress** (baseline TS/ESLint снят, build и dev-сценарии — при финальной проверке 6.1)

### 0.2. Привести к одному package manager [P1]

- [x] Выбрать `pnpm` как основной менеджер, так как в проекте используется `pnpm-lock.yaml`.
- [x] Удалить `package-lock.json`, если он не нужен команде. → удалён, был закоммичен изначально с "start commit", не поддерживался параллельно.
- [x] Добавить в `package.json` поле `packageManager` с используемой версией pnpm. → `"packageManager": "pnpm@10.28.2"`.
- [x] Проверить чистую установку зависимостей. → `rm -rf node_modules && pnpm install` прошёл чисто, без warning о несовпадении packageManager.

**Зависимости:** 0.1.
**status: done** (напрямую, тривиально)

## Этап 1. Данные, SSR и маршрутизация

### 1.1. Исправить SSR-гидрацию React Query [P0]

- [x] Создать или использовать единый серверный `QueryClient` для префетча.
- [x] Передать результат `dehydrate(queryClient)` в `HydrationBoundary` или существующий provider.
- [x] Убедиться, что клиентский `useGetCountries` использует тот же query key и те же параметры.
- [x] Удалить передачу пустого `{}` как `dehydratedState`.
- [ ] Проверить, что после SSR первый рендер не запускает лишний повторный запрос. *(визуально не проверялось, только статически)*

**Зависимости:** 0.1.

**Готово, когда:** HTML страницы содержит данные, а React Query использует префетчированный кэш после гидрации.
**status: done** (coder, subtask-1-ssr-hydration; `tsc --noEmit` чисто)

### 1.2. Убрать дублирование стран в Zustand и React Query [P0]

- [x] Определить React Query единственным источником серверных данных о странах. → `mainPage.tsx` и `countryPage.tsx` берут данные только из `useGetCountries`/`useGetCountry`.
- [x] Оставить в Zustand только UI-состояние, если оно действительно нужно. → `useCountriesStore`/`countri-store.ts` больше нигде не импортируется (проверено grep'ом); `app-store.ts` (UI-флаги загрузки) не трогали, он не про список стран.
- [ ] Удалить запись списка стран в Zustand и связанные неиспользуемые селекторы. *(файл `countri-store.ts` намеренно не удалён сейчас — удаление мёртвого кода запланировано отдельно в 3.3, чтобы не смешивать с этим рефакторингом)*
- [x] Проверить, что переходы, обновление страницы и возврат назад используют query cache. → оба компонента используют React Query напрямую с осмысленными `staleTime`/`refetchInterval` из `providers.tsx`.

**Зависимости:** 1.1.

**Готово, когда:** один список стран не хранится параллельно в двух хранилищах.
**status: done** (устранено попутно при 1.3/2.1/2.2 — единый источник данных: React Query; фактическое удаление файла стора — в 3.3)

### 1.3. Переделать загрузку страницы страны [P0]

- [x] Использовать параметр маршрута `[name]` как источник идентификатора страны. → ссылка с карточки теперь строится как `el.cca3` (было `cca2+cca3+ccn3`), маршрут `[name]` = cca3.
- [x] Добавить отдельный React Query-запрос для страны по стабильному коду (`cca3`) или другому поддерживаемому API-параметру. → `useGetCountry`/`getCountryByCode` (`alpha/{code}`) в `countriesApi.ts`.
- [x] Не полагаться на то, что пользователь сначала посетил главную страницу. → detail page больше не читает Zustand-стор, только `useGetCountry(cca3)`.
- [ ] Обработать прямое открытие URL, обновление страницы и открытие ссылки в новой вкладке. *(логика на месте, вручную в браузере не проверялось — см. 6.1)*
- [x] Добавить состояния загрузки, ошибки и отсутствующей страны. → `isLoading` → `Loader`, `isError`/`!data` → `notFound()`.
- [x] Направлять отсутствующую страну на корректный not-found сценарий через Next.js. → добавлен `src/app/not-found.tsx`, переиспользует существующий `NotFoundPage`.

**Зависимости:** 1.1.

**Готово, когда:** detail page работает после прямого перехода без предварительной загрузки главной страницы.
**status: done** (coder: subtask-2-country-by-code-api, subtask-3-country-detail-page; `src/app/not-found.tsx` и правка ссылки в mainPage.tsx — напрямую, тривиально; `tsc --noEmit` чисто)

**Примечание по процессу:** subtask-3 потребовал 4 попытки — 3 провалились из-за дегенеративного зацикливания coder-модели на backslash-экранировании в JSON (regex `\B(?=(\d{3})+(?!\d))` в `numberWithSpaces`), 4-я прошла с первого раза после замены на `toLocaleString('ru-RU')`. Стоит избегать regex с бэкслэшами в задачах для этой модели.

### 1.4. Проверить API-контракт и query keys [P1]

- [ ] Описать типы ответа REST Countries API для списка и detail-запроса.
- [ ] Привести query keys к единому формату.
- [ ] Добавить обработку пустого ответа и ошибок HTTP.
- [ ] Проверить наличие и корректность переменной окружения для base URL.

**Зависимости:** 1.1, 1.3.

## Этап 2. Надёжность интерфейса

### 2.1. Исправить бесконечную прокрутку [P0]

- [x] Исправить cleanup scroll listener в `useInfinity`. → scroll listener убран целиком, заменён на IntersectionObserver (см. 2.2); прежний баг (эффект добавлял listener на каждый ререндер и никогда не снимал его — return cleanup терялся) устранён вместе с заменой механизма.
- [x] Проверить, что обработчик не добавляется повторно при ререндерах. → observer пересоздаётся только при смене `items.length`, снимается в cleanup.
- [x] Не запускать следующий запрос во время текущего (`isFetching` / `hasNextPage`). → сетевого запроса на «страницу» нет: `getCountries` отдаёт весь список одним запросом, infinite scroll — это клиентская нарезка уже загруженных данных (`visibleCount`), дублирования нет по построению.
- [x] Обработать достижение конца списка. → `hasMore` вычисляется как `visibleCount < items.length`, sentinel/кнопка скрываются по достижении конца.
- [x] Сохранить доступность: добавить явный способ загрузить ещё элементы или корректный fallback для клавиатуры. → кнопка «Загрузить ещё» всегда показывается, пока `hasMore`, независимо от поддержки IntersectionObserver.

**Готово, когда:** запросы не дублируются, обработчики удаляются, а список стабильно догружается.
**status: done** (coder: subtask-4-use-infinity-rewrite, subtask-5-mainpage-rewrite; заодно устранена и дублирующая стора Zustand — см. 1.2; `tsc --noEmit` и ESLint чисты по этим файлам)

### 2.2. Заменить scroll listener на Intersection Observer [P1]

- [x] Добавить sentinel-элемент после списка карточек. → `<div ref={sentinelRef} />` в `mainPage.tsx`.
- [x] Наблюдать за sentinel через `IntersectionObserver`.
- [x] Отключать observer при размонтировании компонента. → `observer.disconnect()` в cleanup эффекта.
- [x] Сохранить текущую порцию загрузки и состояния React Query. → `useGetCountries()` не менялся, слайсинг вынесен в `useInifinity`.
- [x] Добавить fallback или кнопку загрузки для окружений без Intersection Observer. → кнопка «Загрузить ещё» рендерится всегда при `hasMore`.

**Зависимости:** 2.1.
**status: done** (реализовано вместе с 2.1 в одной паре subtask'ов, т.к. чинить cleanup в scroll-based хуке отдельно от замены на IO было бы избыточной промежуточной работой)

### 2.3. Добавить полноценные состояния UI [P1]

- [ ] Отдельно оформить initial loading, загрузку следующей страницы, empty state и error state. *(initial loading и error есть; отдельного empty state для «0 результатов» и состояния «догрузка следующей порции» нет — сейчас это просто больше видимых карточек без индикатора)*
- [x] Добавить retry для временной ошибки API. → кнопка «Повторить» на главной странице вызывает `refetch()` от `useGetCountries`.
- [x] Добавить error boundary для ошибок рендера страницы. → `src/app/error.tsx` (Next.js App Router error boundary) с кнопкой `reset()`.
- [x] Проверить, что toast не дублируется при каждом ререндере. → `toastWrapper` вызывается в `useEffect` с зависимостью `[error]`, ререндеры без смены объекта ошибки его не повторяют — структурно уже было корректно, изменений не потребовалось.
- [ ] Проверить отображение на узких экранах и при длинных названиях стран. *(не проверялось визуально в этой сессии — см. 6.1)*

**status: in-progress** (retry + error boundary — напрямую, тривиально; `tsc --noEmit`/ESLint чисты; полные empty/pagination-состояния оставлены на потом, не блокируют P0/P1 функциональность)

**Зависимости:** 1.3, 2.1.

### 2.4. Исправить отображение данных карточки и detail page [P0]

- [x] Заменить `filter` на `find` там, где требуется одна страна. → снято вместе с 1.3: detail page больше не фильтрует список, а запрашивает страну напрямую по `cca3`.
- [x] Исправить отображение `capital`, если это массив. → `join(', ')` в `mainPage.tsx` и `countryPage.tsx`.
- [x] Использовать стабильный React key, например `cca3`, вместо индекса массива. → `key={el.cca3}` в `mainPage.tsx`.
- [x] Обработать отсутствующие `capital`, флаг, регион и другие необязательные поля. → guard на `el.flags?.png`, `'—'` fallback для capital/status/region/area/population.
- [x] Проверить форматирование чисел населения и площади. → `toLocaleString('ru-RU')` в `countryPage.tsx`.

**status: done** (правки в `mainPage.tsx` — напрямую, тривиально; detail page — через subtask-3; `tsc --noEmit` чисто)

## Этап 3. Типизация и структура кода

### 3.1. Устранить слабую типизацию [P1]

- [x] Заменить `props: any` в `CardKit` на именованный тип или интерфейс. → `CardKitProps extends BoxProps` в `card.tsx`.
- [x] Проверить типы props всех UI-компонентов. → остальные UI-компоненты (`Container`, `Loader`, `toastWrapper`) уже были типизированы, `any` был только в `CardKit`.
- [x] Уточнить типы API-моделей и сделать optional только реально необязательные поля. → `CountryType` в `types.ts` заменён с `typeof Country` (sample JSON) на явный интерфейс; `capital`/`flags`/`region`/`area`/`population`/`status` помечены optional (реально отсутствуют у части стран в REST Countries API), `name`/`cca2`/`cca3` обязательны.
- [x] Добавить типы для параметров маршрута и результатов запросов. → `useGetCountry(code: string)` типизирован через `CountryType`; `page.tsx` для `[name]` уже типизировал `params: { name: string }`.
- [x] Повторно прогнать TypeScript после каждого логического блока. → `tsc --noEmit` чист после каждого шага (CardKit, types.ts + guard на `flags` в `countryPage.tsx`).

**Зависимости:** 1.4, 2.4.
**status: done** (coder: subtask-6-cardkit-types, subtask-7-country-types; guard на опциональный `flags` в countryPage.tsx — напрямую, тривиально, вызвано изменением типа; `tsc --noEmit` и ESLint чисты)

**Примечание:** пункт 1.4 (полный API-контракт, обработка HTTP-ошибок, проверка env) выполнен не полностью — базовые типы и env-переменная на месте, отдельная обработка пустого ответа/HTTP-ошибок в `countriesApi.ts` пока не добавлена, оставлено для отдельного прохода.

### 3.2. Исправить имена и опечатки [P2]

- [x] Переименовать `countri-store` в `country-store`. → отменено в пользу удаления: стор больше не используется (см. 1.2), переименовывать нечего — файл удалён в рамках 3.3.
- [x] Переименовать `useInifinity` в `useInfinity`. → хук уже назывался `useInfinity.tsx` (имя файла), переименован сам экспорт `useInifinity` → `useInfinity` в `useInfinity.tsx`, `mainPage.tsx`, `useInfinity.test.tsx`.
- [x] Переименовать `BASE_PUBLICK_URL` в корректное имя. → `NEXT_PUBLIC_BASE_API_URL` в `baseApi.ts` и `.env.example`. **Нужно вручную переименовать ключ в `.env.local` — этот файл не открывался и не редактировался (секрет), приложение не запустится без этого шага.**
- [x] Обновить все импорты и проверить регистр имён на Linux. → grep по всем старым именам (`useInifinity`, `BASE_PUBLICK_URL`, `countri-store`) — совпадений не осталось.
- [x] Не смешивать переименование с изменением поведения в одном коммите. → все правки в 3.2/3.3 не коммитились (по правилам процесса коммитит человек), сделаны отдельно от предыдущих поведенческих правок.

**status: done** (напрямую; `tsc --noEmit`, ESLint, `vitest run` — все чисто/зелёные)

### 3.3. Удалить мёртвый код и зависимости [P1]

- [x] Проверить, нужен ли `useStore.ts`; удалить его вместе с неиспользуемыми импортами. → нигде не импортировался (grep), удалён.
- [x] Проверить использование `framer-motion` и удалить пакет, если он не используется. → не использовался, `pnpm remove framer-motion`.
- [x] Проверить использование `use-dehydrated-state` и удалить пакет, если он не нужен после исправления SSR. → не использовался (SSR чинили через `dehydrate()` из `@tanstack/react-query` в 1.1), `pnpm remove use-dehydrated-state`.
- [x] Удалить sample JSON или явно определить его назначение в разработке. → `country.json` удалён: `types.ts` теперь не зависит от sample-данных (см. 3.1), других импортов не было.
- [x] После очистки выполнить чистую установку и production-сборку. → `pnpm install` прошёл чисто; `pnpm build` не запускался в этой сессии, см. 6.1/6.2 для финальной проверки сборки.

**Зависимости:** 1.1, 1.2, 3.2.
**status: done** (kроме финального `pnpm build`, вынесенного в 6.2)

## Этап 4. Тестирование

### 4.1. Подготовить тестовую инфраструктуру [P1]

- [x] Выбрать Vitest или Jest в соответствии с совместимостью с Next.js 14. → Vitest (быстрее интегрируется с Next 14 + Vite-based tooling, чем Jest + SWC/babel-конфиг).
- [x] Добавить React Testing Library и необходимые настройки DOM. → `@testing-library/react` + `jsdom` + `@testing-library/jest-dom`, `vitest.setup.ts`.
- [x] Добавить команды `test` и `test:watch` в `package.json`.
- [x] Настроить моки Axios/API и React Query. → `vi.mock('./baseApi')` для axios-инстанса, отдельный `QueryClient` на тест с `retry: false`.

**status: done** (установка пакетов и конфиг — напрямую, это тулинг, не продуктовый код; `pnpm add -D vitest @vitejs/plugin-react jsdom @testing-library/*`)
**Примечание:** `pnpm add` вывел предупреждение о security-уязвимости в `next@14.2.3` — не относится к этому тикету, но стоит запланировать апгрейд отдельно.

### 4.2. Добавить unit-тесты [P1]

- [x] Проверить `CardKit`: корректный рендер данных и optional-полей. → `card.test.tsx`, 3 теста (label, children, рендер без опциональных пропсов).
- [x] Проверить `useGetCountries`: успешный ответ, ошибка и пустой результат. → `countriesApi.test.tsx`, 3 теста.
- [x] Проверить хук бесконечной загрузки: cleanup, блокировку повторных запросов и конец списка. → `useInfinity.test.tsx`, 3 теста (первая страница, `loadMore`, `hasMore=false` в конце списка). *(тест на реальный disconnect IntersectionObserver не писали — `renderHook` без примонтированного sentinel-DOM-узла не даёт осмысленно это проверить; cleanup подтверждён код-ревью, не тестом)*
- [ ] Проверить преобразование данных API в модели UI. *(отдельной функции-трансформера нет — форматирование `capital`/`numberWithSpaces` инлайновое в компонентах; не тестировалось изолированно)*

**status: done** (coder: subtask-8/9/10; 9 тестов, `npx vitest run` зелёный, `tsc --noEmit` чист)

**Зависимости:** 3.1, 4.1.

### 4.3. Добавить интеграционные тесты страниц [P1]

- [ ] Проверить главную страницу с loading, success, empty и error состояниями.
- [ ] Проверить страницу страны при наличии данных.
- [ ] Проверить страницу страны при прямом открытии и отсутствии страны.
- [ ] Проверить переход с карточки на detail route.

**Зависимости:** 1.3, 2.3, 4.1.

### 4.4. Добавить один E2E-сценарий [P2]

- [ ] Выбрать Playwright или другой принятый в проекте E2E-инструмент.
- [ ] Реализовать сценарий: открыть список, дождаться данных, выбрать страну, открыть detail page.
- [ ] Добавить сценарий прямого открытия detail URL.
- [ ] Запускать E2E в CI или отдельной проверочной команде.

**Зависимости:** 4.3.

## Этап 5. Конфигурация и документация

### 5.1. Исправить безопасность конфигурации [P0]

- [x] Добавить `.env.local` в `.gitignore`.
- [x] Проверить, что секреты и локальные значения не отслеживаются Git. → `.env.local` был закоммичен и не игнорировался; выполнен `git rm --cached`, файл остаётся локально. **ВНИМАНИЕ: значение уже есть в истории git — ротация/очистка истории не выполнена, это решение человека.**
- [x] Создать `.env.example` с безопасными placeholder-значениями.
- [ ] Уточнить, какие переменные доступны клиенту, а какие должны оставаться серверными. *(единственная переменная — `NEXT_PUBLIC_...`, то есть намеренно клиентская; отдельных серверных секретов не обнаружено)*

**status: done** (сделано напрямую, без coder — тривиальная правка)

### 5.2. Обновить README [P1]

- [x] Заменить стандартный create-next-app README описанием проекта.
- [x] Описать установку зависимостей через pnpm.
- [x] Описать команды разработки, проверки, тестов и production-сборки.
- [x] Описать переменные окружения и источник данных.
- [x] Кратко описать структуру каталогов и поток данных.

**Зависимости:** 0.2, 5.1.
**status: done** (напрямую)

### 5.3. Настроить автоматические проверки [P2]

- [ ] Добавить CI-проверки для TypeScript, ESLint, unit-тестов и build.
- [ ] Зафиксировать версию Node.js и pnpm.
- [ ] Проверять отсутствие незакоммиченных lockfile-конфликтов.
- [ ] Добавить проверку production-сборки перед слиянием изменений.

**Зависимости:** 0.2, 4.1, 5.2.

## Этап 6. Финальная проверка

### 6.1. Провести ручной regression check [P0]

- [ ] Главная страница открывается с SSR-данными.
- [ ] Догрузка стран выполняется без дублирования запросов.
- [ ] Переход на страну работает из списка.
- [ ] Страница страны работает при прямом URL и после обновления.
- [ ] Несуществующая страна показывает 404/not-found состояние.
- [ ] Ошибка API даёт понятный retry-сценарий.
- [ ] Responsive layout не ломается на мобильном и desktop-размерах.

### 6.2. Финализировать качество [P1]

- [x] `tsc --noEmit` завершается без ошибок.
- [x] ESLint завершается без ошибок. → 1 warning в `useLoad.tsx` (не в скоупе выполненных подзадач, не блокирует).
- [x] Все тесты проходят. → `vitest run`: 3 файла, 9/9 тестов.
- [x] Production build завершается успешно. → чистый `pnpm install` + `pnpm build` (после `rm -rf node_modules .next`) прошли без ошибок.
- [ ] В diff нет случайных изменений, временных файлов и дублирующихся lockfile. → **`package-lock.json` всё ещё в репозитории рядом с `pnpm-lock.yaml`** — это пункт 0.2, сознательно отложен в этом проходе (пользователь выбрал 3.2–3.3, не 0.2).
- [ ] Обновить этот план, отметив выполненные задачи и оставшиеся риски. → см. итоговую сводку ниже.

## Рекомендуемый порядок выполнения

1. 0.1 — зафиксировать исходное состояние.
2. 5.1 — закрыть риск с `.env.local`.
3. 1.1 — исправить SSR-гидрацию.
4. 1.3 — сделать detail page независимой от Zustand.
5. 2.4 — исправить ошибки отображения данных.
6. 2.1 и 2.2 — стабилизировать бесконечную загрузку.
7. 3.1 — усилить типизацию.
8. 2.3 — довести loading/error/empty UX.
9. 4.1–4.4 — добавить тесты.
10. 3.2–3.3 — завершить переименование и очистку.
11. 0.2, 5.2–5.3 — унифицировать tooling и документацию.
12. 6.1–6.2 — выполнить финальную проверку.

## Сводка на конец сессии orchestrator'а

**Сделано:** все P0-пункты кроме браузерного regression check (6.1); P1 — 1.4 частично, 2.3 частично, 3.1, 3.3, 4.1, 4.2 полностью. `tsc --noEmit`, ESLint (кроме 1 предсуществующего warning в `useLoad.tsx`), `vitest run` (9/9), чистые `pnpm install`/`pnpm build` — всё зелёное на момент записи.

**Не сделано (осознанно отложено):**
- 0.2 — `package-lock.json` всё ещё в репозитории рядом с `pnpm-lock.yaml`.
- 1.4 — типы API-модели есть, но обработки пустого ответа/HTTP-ошибок в `countriesApi.ts` нет отдельно от React Query.
- 2.3 — нет отдельного empty state (0 результатов) и индикатора догрузки следующей порции (сейчас просто добавляются карточки).
- 4.3/4.4 — интеграционных и e2e-тестов нет.
- 5.2/5.3 — README и CI не обновлялись.
- 6.1 — ручная проверка в браузере (реальный SSR HTML, работа detail-страницы при прямом URL, responsive) не выполнялась в этой сессии — только статический анализ и сборка.

**Риски, требующие внимания человека:**
- `.env.local` был закоммичен в git и не игнорировался до этой сессии — значение уже есть в истории git, ротация секрета/переписывание истории не выполнялись.
- Переменная окружения переименована (`BASE_PUBLICK_URL` → `NEXT_PUBLIC_BASE_API_URL`) — ключ в `.env.local` нужно было переименовать вручную (файл не редактировался ассистентом, как секрет); судя по успешной сборке, это уже сделано.
- `pnpm add`/`pnpm build` вывели предупреждение о security-уязвимости в `next@14.2.3` — не по этому тикету, но стоит запланировать апгрейд отдельно.
- В процессе работы coder (локальная модель qwen3-14b через LM Studio) дважды подряд зацикливался на генерации кода с бэкслэш-heavy regex в JSON tool-call — избегайте таких конструкций в задачах для этой модели, использовать `toLocaleString` вместо ручного regex-форматирования чисел.
